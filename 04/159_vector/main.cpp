#include <vector> // Для std::vector
#include <string> // Для std::string

int main() {
    std::vector<int> v = {5, 7, 9, 4, 6, 8};
    std::vector<std::string> plilosopher = {"Kant", "Plato", "Hume", "Kierkegaard"};
    std::vector<int> vi(6);
    std::vector<std::string> vs(4);
    return 0;
}
